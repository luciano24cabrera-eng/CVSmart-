# CVSmart V2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rewrite CVSmart in Python/FastAPI with redesigned frontend, automatic candidate feedback email, and AI-powered CV creator with PDF download.

**Architecture:** Single FastAPI process on port 8000. Backend modules: `database.py`, `analyzer.py`, `email_sender.py`, `cv_generator.py`. Frontend: 4 static HTML pages served directly by FastAPI. No Node.js.

**Tech Stack:** Python 3.11, FastAPI, uvicorn, SQLite (stdlib), pdfplumber, groq-sdk, reportlab, smtplib, python-dotenv, python-multipart

---

## File Map

| File | Action | Responsibility |
|------|--------|---------------|
| `requirements.txt` | Create | Python deps |
| `.env.example` | Create | Env var template |
| `database.py` | Create | SQLite init + all queries |
| `analyzer.py` | Create | PDF text extraction + Groq CV scoring |
| `email_sender.py` | Create | Gmail SMTP + HTML email template |
| `cv_generator.py` | Create | Groq CV improvement + reportlab PDF |
| `main.py` | Create | FastAPI app, all routes, static serving |
| `tests/test_analyzer.py` | Create | Unit tests for score_label + JSON parsing |
| `tests/test_email_sender.py` | Create | Unit tests for HTML email builder |
| `tests/test_cv_generator.py` | Create | Unit tests for PDF generation |
| `frontend/index.html` | Create | Landing page |
| `frontend/aplicar.html` | Create | Candidate application form |
| `frontend/panel.html` | Create | Recruiter dashboard |
| `frontend/crear-cv.html` | Create | AI CV creator |

---

## Task 1: Project Setup

**Files:** `requirements.txt`, `.env.example`, `cvs/`, `tests/`, `frontend/`

- [ ] **Step 1: Create requirements.txt**

```
fastapi
uvicorn[standard]
python-multipart
pdfplumber
groq
reportlab
python-dotenv
pytest
httpx
```

- [ ] **Step 2: Create .env.example**

```
GROQ_API_KEY=tu_clave_aqui
SECRET_PANEL=cvsmart2026
GMAIL_USER=tu_correo@gmail.com
GMAIL_APP_PASSWORD=tu_app_password_aqui
GROQ_MODEL=llama-3.3-70b-versatile
```

- [ ] **Step 3: Create directories**

```bash
mkdir -p cvs frontend tests
touch tests/__init__.py
```

- [ ] **Step 4: Install dependencies**

```bash
pip install -r requirements.txt
```

Expected: all packages install without errors.

- [ ] **Step 5: Commit**

```bash
git add requirements.txt .env.example
git commit -m "chore: add Python/FastAPI project setup"
```

---

## Task 2: database.py

**Files:** `database.py`, `tests/test_database.py`

- [ ] **Step 1: Write failing test**

Create `tests/test_database.py`:

```python
import os, pytest
os.environ.setdefault("GROQ_API_KEY", "test")

from database import init_db, insert_candidate, get_all_candidates, get_stats, get_candidate, mark_email_sent
import database, sqlite3
from pathlib import Path

@pytest.fixture(autouse=True)
def tmp_db(tmp_path, monkeypatch):
    monkeypatch.setattr(database, "DB_PATH", tmp_path / "test.db")
    init_db()

def test_init_creates_table(tmp_path, monkeypatch):
    monkeypatch.setattr(database, "DB_PATH", tmp_path / "t.db")
    init_db()
    conn = sqlite3.connect(tmp_path / "t.db")
    tables = conn.execute("SELECT name FROM sqlite_master WHERE type='table'").fetchall()
    assert ("candidates",) in tables

def test_insert_and_get():
    cid = insert_candidate(
        name="Juan Pérez", email="j@test.com", phone="", cv_filename="cv.pdf",
        cv_original_name="cv.pdf", score=8.0, score_label="Excelente",
        years_experience=5, education_level="Licenciatura",
        matching_skills='["Python"]', summary="Resumen", strength="Técnico",
        weaknesses='["Área 1"]', full_analysis='{}',
        availability="Inmediata", expected_salary="30000", specific_experience="Dev"
    )
    assert cid > 0
    candidate = get_candidate(cid)
    assert candidate["name"] == "Juan Pérez"
    assert candidate["matching_skills"] == ["Python"]

def test_get_stats():
    insert_candidate(
        name="A", email="a@test.com", phone="", cv_filename="a.pdf",
        cv_original_name="a.pdf", score=9.0, score_label="Excelente",
        years_experience=3, education_level="Maestría",
        matching_skills='[]', summary="", strength="", weaknesses='[]',
        full_analysis='{}', availability="Inmediata", expected_salary="40000",
        specific_experience=""
    )
    stats = get_stats()
    assert stats["total"] == 1
    assert stats["high_score"] == 1

def test_mark_email_sent():
    cid = insert_candidate(
        name="B", email="b@test.com", phone="", cv_filename="b.pdf",
        cv_original_name="b.pdf", score=6.0, score_label="Bueno",
        years_experience=2, education_level="Técnico",
        matching_skills='[]', summary="", strength="", weaknesses='[]',
        full_analysis='{}', availability="2 semanas", expected_salary="20000",
        specific_experience=""
    )
    mark_email_sent(cid)
    assert get_candidate(cid)["email_sent"] == 1
```

- [ ] **Step 2: Run test — expect FAIL**

```bash
pytest tests/test_database.py -v
```

Expected: `ModuleNotFoundError: No module named 'database'`

- [ ] **Step 3: Create database.py**

```python
import sqlite3, json
from pathlib import Path
from typing import Optional

DB_PATH = Path(__file__).parent / "cvsmart.db"

def _conn():
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row
    return conn

def init_db():
    with _conn() as conn:
        conn.execute("""
            CREATE TABLE IF NOT EXISTS candidates (
                id INTEGER PRIMARY KEY AUTOINCREMENT,
                name TEXT NOT NULL,
                email TEXT,
                phone TEXT,
                cv_filename TEXT NOT NULL,
                cv_original_name TEXT,
                score REAL DEFAULT 0,
                score_label TEXT,
                years_experience REAL,
                education_level TEXT,
                matching_skills TEXT DEFAULT '[]',
                summary TEXT,
                strength TEXT,
                weaknesses TEXT DEFAULT '[]',
                full_analysis TEXT DEFAULT '{}',
                availability TEXT,
                expected_salary TEXT,
                specific_experience TEXT,
                email_sent INTEGER DEFAULT 0,
                created_at DATETIME DEFAULT CURRENT_TIMESTAMP
            )
        """)
        conn.commit()

def insert_candidate(**kwargs) -> int:
    with _conn() as conn:
        cur = conn.execute("""
            INSERT INTO candidates (
                name, email, phone, cv_filename, cv_original_name,
                score, score_label, years_experience, education_level,
                matching_skills, summary, strength, weaknesses,
                full_analysis, availability, expected_salary, specific_experience
            ) VALUES (
                :name, :email, :phone, :cv_filename, :cv_original_name,
                :score, :score_label, :years_experience, :education_level,
                :matching_skills, :summary, :strength, :weaknesses,
                :full_analysis, :availability, :expected_salary, :specific_experience
            )
        """, kwargs)
        conn.commit()
        return cur.lastrowid

def get_all_candidates() -> list:
    with _conn() as conn:
        rows = conn.execute("""
            SELECT id, name, email, phone, score, score_label, years_experience,
                   education_level, matching_skills, summary, strength, weaknesses,
                   availability, expected_salary, specific_experience,
                   cv_original_name, email_sent, created_at
            FROM candidates ORDER BY score DESC, created_at DESC
        """).fetchall()
        result = []
        for row in rows:
            d = dict(row)
            d["matching_skills"] = json.loads(d["matching_skills"] or "[]")
            d["weaknesses"] = json.loads(d["weaknesses"] or "[]")
            result.append(d)
        return result

def get_stats() -> dict:
    with _conn() as conn:
        row = conn.execute("""
            SELECT COUNT(*) as total,
                   ROUND(AVG(score), 1) as avg_score,
                   COUNT(CASE WHEN score >= 8 THEN 1 END) as high_score,
                   COUNT(CASE WHEN score >= 5 AND score < 8 THEN 1 END) as mid_score,
                   COUNT(CASE WHEN score < 5 THEN 1 END) as low_score
            FROM candidates
        """).fetchone()
        return dict(row)

def get_candidate(candidate_id: int) -> Optional[dict]:
    with _conn() as conn:
        row = conn.execute("SELECT * FROM candidates WHERE id = ?", (candidate_id,)).fetchone()
        if not row:
            return None
        d = dict(row)
        d["matching_skills"] = json.loads(d["matching_skills"] or "[]")
        d["weaknesses"] = json.loads(d["weaknesses"] or "[]")
        d["full_analysis"] = json.loads(d["full_analysis"] or "{}")
        return d

def mark_email_sent(candidate_id: int):
    with _conn() as conn:
        conn.execute("UPDATE candidates SET email_sent = 1 WHERE id = ?", (candidate_id,))
        conn.commit()
```

- [ ] **Step 4: Run tests — expect PASS**

```bash
pytest tests/test_database.py -v
```

Expected: 4 tests PASSED.

- [ ] **Step 5: Commit**

```bash
git add database.py tests/test_database.py
git commit -m "feat: add database module with SQLite init and queries"
```

---

## Task 3: analyzer.py

**Files:** `analyzer.py`, `tests/test_analyzer.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_analyzer.py`:

```python
import json, pytest
from analyzer import score_label, _parse_analysis

def test_score_label_excelente():
    assert score_label(10) == "Excelente"
    assert score_label(8) == "Excelente"

def test_score_label_bueno():
    assert score_label(7) == "Bueno"
    assert score_label(5) == "Bueno"

def test_score_label_en_desarrollo():
    assert score_label(4) == "En desarrollo"
    assert score_label(1) == "En desarrollo"

def test_parse_valid_json():
    raw = json.dumps({
        "nombre": "Juan Pérez", "años_experiencia": 5,
        "nivel_estudios": "Licenciatura", "habilidades_coincidentes": ["Python"],
        "puntaje": 8, "resumen": "Buen perfil",
        "fortaleza": "Técnico fuerte", "debilidades": ["Área 1", "Área 2"]
    })
    result = _parse_analysis(raw)
    assert result["nombre"] == "Juan Pérez"
    assert result["score_label"] == "Excelente"

def test_parse_json_embedded_in_text():
    raw = 'Texto antes {"nombre": "Ana", "años_experiencia": 2, "nivel_estudios": "Maestría", "habilidades_coincidentes": [], "puntaje": 6, "resumen": "ok", "fortaleza": "comunicación", "debilidades": ["d1", "d2"]} texto después'
    result = _parse_analysis(raw)
    assert result["nombre"] == "Ana"
    assert result["score_label"] == "Bueno"

def test_parse_fallback_on_bad_json():
    result = _parse_analysis("esto no es json")
    assert result["nombre"] == "No identificado"
    assert result["puntaje"] == 5
```

- [ ] **Step 2: Run — expect FAIL**

```bash
pytest tests/test_analyzer.py -v
```

Expected: `ModuleNotFoundError: No module named 'analyzer'`

- [ ] **Step 3: Create analyzer.py**

```python
import os, re, json, io
import pdfplumber
from groq import Groq

client = Groq(api_key=os.getenv("GROQ_API_KEY"))

PROMPT = """Eres un experto en recursos humanos. Analiza el siguiente CV.
Responde ÚNICAMENTE con un objeto JSON válido (sin markdown, sin texto adicional) con esta estructura exacta:
{
  "nombre": "nombre completo del candidato",
  "años_experiencia": 0,
  "nivel_estudios": "nivel académico más alto",
  "habilidades_coincidentes": ["habilidad1", "habilidad2"],
  "puntaje": 7,
  "resumen": "resumen ejecutivo del candidato",
  "fortaleza": "principal fortaleza detectada",
  "debilidades": ["área de oportunidad 1", "área de oportunidad 2"]
}
Criterio de puntaje: 9-10 excepcional, 7-8 muy bueno, 5-6 adecuado, 3-4 con carencias, 1-2 no apto.
CV a analizar:
{CV_TEXT}"""

def score_label(score: float) -> str:
    if score >= 8:
        return "Excelente"
    if score >= 5:
        return "Bueno"
    return "En desarrollo"

def _parse_analysis(raw: str) -> dict:
    try:
        match = re.search(r'\{[\s\S]*\}', raw)
        analysis = json.loads(match.group(0) if match else raw)
        analysis["score_label"] = score_label(analysis.get("puntaje", 5))
        return analysis
    except Exception:
        return {
            "nombre": "No identificado", "años_experiencia": 0,
            "nivel_estudios": "No especificado", "habilidades_coincidentes": [],
            "puntaje": 5, "score_label": "Bueno",
            "resumen": "Análisis no disponible.", "fortaleza": "",
            "debilidades": ["Revisar CV manualmente", "Formato no procesable"]
        }

def extract_text(pdf_bytes: bytes) -> str:
    with pdfplumber.open(io.BytesIO(pdf_bytes)) as pdf:
        return "\n".join(page.extract_text() or "" for page in pdf.pages)

def analyze_cv(pdf_bytes: bytes) -> dict:
    text = extract_text(pdf_bytes)
    if not text or len(text.strip()) < 30:
        raise ValueError("El PDF no contiene texto legible.")
    prompt = PROMPT.replace("{CV_TEXT}", text[:12000])
    completion = client.chat.completions.create(
        model=os.getenv("GROQ_MODEL", "llama-3.3-70b-versatile"),
        max_tokens=1024,
        messages=[{"role": "user", "content": prompt}],
    )
    return _parse_analysis(completion.choices[0].message.content.strip())
```

- [ ] **Step 4: Run tests — expect PASS**

```bash
pytest tests/test_analyzer.py -v
```

Expected: 5 tests PASSED.

- [ ] **Step 5: Commit**

```bash
git add analyzer.py tests/test_analyzer.py
git commit -m "feat: add CV analyzer with Groq scoring and text extraction"
```

---

## Task 4: email_sender.py

**Files:** `email_sender.py`, `tests/test_email_sender.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_email_sender.py`:

```python
from email_sender import build_email_html

def test_contains_name():
    html = build_email_html("Juan Pérez", "Excelente", "Técnico fuerte", ["D1", "D2"])
    assert "Juan Pérez" in html

def test_contains_score_label():
    html = build_email_html("Ana", "Bueno", "Comunicación", ["D1", "D2"])
    assert "Bueno" in html

def test_contains_both_weaknesses():
    html = build_email_html("Carlos", "En desarrollo", "Puntual", ["Mejorar redacción", "Agregar certificaciones"])
    assert "Mejorar redacción" in html
    assert "Agregar certificaciones" in html

def test_contains_strength():
    html = build_email_html("Laura", "Excelente", "Liderazgo excepcional", ["D1", "D2"])
    assert "Liderazgo excepcional" in html

def test_is_valid_html():
    html = build_email_html("Test", "Bueno", "F", ["D1", "D2"])
    assert "<!DOCTYPE html>" in html
    assert "</html>" in html
```

- [ ] **Step 2: Run — expect FAIL**

```bash
pytest tests/test_email_sender.py -v
```

- [ ] **Step 3: Create email_sender.py**

```python
import os, smtplib
from email.mime.multipart import MIMEMultipart
from email.mime.text import MIMEText

_BADGE = {
    "Excelente": ("#166534", "#dcfce7"),
    "Bueno":     ("#92400e", "#fef3c7"),
    "En desarrollo": ("#991b1b", "#fee2e2"),
}

def build_email_html(name: str, score_label: str, strength: str, weaknesses: list) -> str:
    color, bg = _BADGE.get(score_label, ("#374151", "#f3f4f6"))
    wk_items = "".join(f"<li style='margin-bottom:6px'>{w}</li>" for w in weaknesses)
    return f"""<!DOCTYPE html>
<html lang="es">
<head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"></head>
<body style="margin:0;padding:0;background:#F8FAFF;font-family:Arial,sans-serif">
  <div style="max-width:600px;margin:32px auto;background:#fff;border-radius:16px;overflow:hidden;box-shadow:0 4px 24px rgba(31,56,100,.1)">
    <div style="background:linear-gradient(135deg,#1F3864,#2E75B6);padding:32px;text-align:center">
      <h1 style="color:#fff;margin:0;font-size:26px">🧠 CVSmart</h1>
      <p style="color:rgba(255,255,255,.8);margin:8px 0 0">Sistema inteligente de filtrado de CVs</p>
    </div>
    <div style="padding:32px">
      <p style="font-size:16px">Hola <strong>{name}</strong>,</p>
      <p style="color:#4b5563">Recibimos tu CV y lo analizamos con nuestra IA. Aquí está tu retroalimentación personalizada:</p>
      <div style="margin:24px 0">
        <p style="font-weight:bold;color:#1F3864;margin-bottom:8px">Tu nivel de perfil:</p>
        <span style="background:{bg};color:{color};padding:8px 20px;border-radius:20px;font-weight:bold;font-size:15px">{score_label}</span>
      </div>
      <div style="margin:24px 0">
        <p style="font-weight:bold;color:#1F3864;margin-bottom:8px">Tu principal fortaleza:</p>
        <p style="background:#F0F7FF;border-left:4px solid #2E75B6;padding:12px 16px;border-radius:0 8px 8px 0;margin:0">{strength}</p>
      </div>
      <div style="margin:24px 0">
        <p style="font-weight:bold;color:#1F3864;margin-bottom:8px">Áreas de oportunidad:</p>
        <ul style="color:#4b5563;padding-left:20px">{wk_items}</ul>
      </div>
      <p style="color:#4b5563">¿Quieres un CV más profesional? Usa nuestro Creador de CV con IA:</p>
      <a href="http://localhost:8000/crear-cv"
         style="display:block;background:linear-gradient(135deg,#1F3864,#2E75B6);color:#fff;text-decoration:none;padding:14px 32px;border-radius:12px;text-align:center;font-weight:bold;margin:16px 0">
        Crear mi CV profesional →
      </a>
    </div>
    <div style="background:#F8FAFF;padding:16px;text-align:center;color:#9ca3af;font-size:12px">
      <p style="margin:0">Equipo CVSmart &copy; 2026 — Este correo fue generado automáticamente.</p>
    </div>
  </div>
</body>
</html>"""

def send_feedback_email(to_email: str, name: str, score_label: str, strength: str, weaknesses: list) -> bool:
    gmail_user = os.getenv("GMAIL_USER")
    gmail_password = os.getenv("GMAIL_APP_PASSWORD")
    if not gmail_user or not gmail_password:
        print("⚠️  Email no enviado: GMAIL_USER o GMAIL_APP_PASSWORD no configurados en .env")
        return False
    msg = MIMEMultipart("alternative")
    msg["Subject"] = "CVSmart — Recibimos tu CV, aquí está tu retroalimentación"
    msg["From"] = gmail_user
    msg["To"] = to_email
    msg.attach(MIMEText(build_email_html(name, score_label, strength, weaknesses), "html"))
    try:
        with smtplib.SMTP_SSL("smtp.gmail.com", 465) as server:
            server.login(gmail_user, gmail_password)
            server.sendmail(gmail_user, to_email, msg.as_string())
        return True
    except Exception as e:
        print(f"⚠️  Error enviando email a {to_email}: {e}")
        return False
```

- [ ] **Step 4: Run tests — expect PASS**

```bash
pytest tests/test_email_sender.py -v
```

- [ ] **Step 5: Commit**

```bash
git add email_sender.py tests/test_email_sender.py
git commit -m "feat: add email sender with HTML template"
```

---

## Task 5: cv_generator.py

**Files:** `cv_generator.py`, `tests/test_cv_generator.py`

- [ ] **Step 1: Write failing tests**

Create `tests/test_cv_generator.py`:

```python
from cv_generator import generate_cv_pdf

SAMPLE_CV = {
    "nombre": "Juan Pérez",
    "contacto": {"email": "juan@example.com", "telefono": "5551234567"},
    "puesto_objetivo": "Desarrollador Python",
    "resumen_profesional": "Profesional con 5 años de experiencia en desarrollo backend.",
    "experiencia": [{
        "empresa": "TechCo", "puesto": "Desarrollador Senior",
        "periodo": "2020–2024",
        "logros": ["Desarrollé API REST para 50k usuarios", "Reduje latencia en 40%"]
    }],
    "educacion": [{"institucion": "UNAM", "carrera": "Ing. en Sistemas", "año": "2019"}],
    "habilidades_tecnicas": ["Python", "FastAPI", "PostgreSQL"],
    "habilidades_blandas": ["Trabajo en equipo", "Comunicación efectiva"],
    "idiomas": ["Español (nativo)", "Inglés (B2)"]
}

def test_returns_bytes():
    pdf = generate_cv_pdf(SAMPLE_CV)
    assert isinstance(pdf, bytes)

def test_is_valid_pdf():
    pdf = generate_cv_pdf(SAMPLE_CV)
    assert pdf[:4] == b"%PDF"

def test_has_content():
    pdf = generate_cv_pdf(SAMPLE_CV)
    assert len(pdf) > 1000
```

- [ ] **Step 2: Run — expect FAIL**

```bash
pytest tests/test_cv_generator.py -v
```

- [ ] **Step 3: Create cv_generator.py**

```python
import os, re, json, io
from groq import Groq
from reportlab.lib.pagesizes import letter
from reportlab.lib import colors
from reportlab.lib.styles import ParagraphStyle
from reportlab.lib.units import inch
from reportlab.lib.enums import TA_LEFT
from reportlab.platypus import SimpleDocTemplate, Paragraph, Spacer, HRFlowable

client = Groq(api_key=os.getenv("GROQ_API_KEY"))

IMPROVE_PROMPT = """Eres un experto en recursos humanos y redacción de CVs profesionales en México.
Con los siguientes datos del candidato, genera un CV completo, profesional y bien redactado.
Mejora la redacción manteniendo la información original.
Devuelve SOLO un JSON válido sin markdown con esta estructura exacta:
{
  "nombre": "",
  "contacto": {"email": "", "telefono": ""},
  "puesto_objetivo": "",
  "resumen_profesional": "párrafo mejorado y profesional",
  "experiencia": [{"empresa": "", "puesto": "", "periodo": "", "logros": ["logro 1", "logro 2"]}],
  "educacion": [{"institucion": "", "carrera": "", "año": ""}],
  "habilidades_tecnicas": [],
  "habilidades_blandas": [],
  "idiomas": []
}
Datos del candidato: {datos}"""

def improve_cv_with_ai(cv_data: dict) -> dict:
    prompt = IMPROVE_PROMPT.replace("{datos}", json.dumps(cv_data, ensure_ascii=False))
    completion = client.chat.completions.create(
        model=os.getenv("GROQ_MODEL", "llama-3.3-70b-versatile"),
        max_tokens=2048,
        messages=[{"role": "user", "content": prompt}],
    )
    raw = completion.choices[0].message.content.strip()
    match = re.search(r'\{[\s\S]*\}', raw)
    return json.loads(match.group(0) if match else raw)

def generate_cv_pdf(cv: dict) -> bytes:
    buffer = io.BytesIO()
    doc = SimpleDocTemplate(buffer, pagesize=letter,
                            leftMargin=0.75*inch, rightMargin=0.75*inch,
                            topMargin=0.75*inch, bottomMargin=0.75*inch)
    navy  = colors.HexColor("#1F3864")
    blue  = colors.HexColor("#2E75B6")
    gray  = colors.HexColor("#4b5563")

    def style(name, **kw):
        defaults = dict(fontName="Helvetica", fontSize=10, leading=14, textColor=gray)
        defaults.update(kw)
        return ParagraphStyle(name, **defaults)

    name_st    = style("Name",    fontName="Helvetica-Bold", fontSize=22, textColor=navy, leading=26)
    contact_st = style("Contact", fontSize=10, textColor=blue)
    section_st = style("Section", fontName="Helvetica-Bold", fontSize=12, textColor=navy, spaceBefore=14)
    body_st    = style("Body")
    bullet_st  = style("Bullet",  leftIndent=12)

    def hr(): return HRFlowable(width="100%", color=blue, thickness=1, spaceAfter=6)

    story = []
    story.append(Paragraph(cv.get("nombre", ""), name_st))
    c = cv.get("contacto", {})
    story.append(Paragraph(f"{c.get('email','')}  |  {c.get('telefono','')}", contact_st))
    if cv.get("puesto_objetivo"):
        story.append(Paragraph(f"<b>Puesto objetivo:</b> {cv['puesto_objetivo']}", body_st))
    story.append(HRFlowable(width="100%", color=navy, thickness=2, spaceBefore=8, spaceAfter=12))

    if cv.get("resumen_profesional"):
        story.append(Paragraph("Resumen Profesional", section_st)); story.append(hr())
        story.append(Paragraph(cv["resumen_profesional"], body_st))

    if cv.get("experiencia"):
        story.append(Paragraph("Experiencia Laboral", section_st)); story.append(hr())
        for exp in cv["experiencia"]:
            story.append(Paragraph(f"<b>{exp.get('puesto','')}</b> — {exp.get('empresa','')} ({exp.get('periodo','')})", body_st))
            for logro in exp.get("logros", []):
                story.append(Paragraph(f"• {logro}", bullet_st))
            story.append(Spacer(1, 6))

    if cv.get("educacion"):
        story.append(Paragraph("Educación", section_st)); story.append(hr())
        for edu in cv["educacion"]:
            story.append(Paragraph(f"<b>{edu.get('carrera','')}</b> — {edu.get('institucion','')} ({edu.get('año','')})", body_st))

    if cv.get("habilidades_tecnicas"):
        story.append(Paragraph("Habilidades Técnicas", section_st)); story.append(hr())
        story.append(Paragraph("  •  ".join(cv["habilidades_tecnicas"]), body_st))

    if cv.get("habilidades_blandas"):
        story.append(Paragraph("Habilidades Blandas", section_st)); story.append(hr())
        story.append(Paragraph("  •  ".join(cv["habilidades_blandas"]), body_st))

    if cv.get("idiomas"):
        story.append(Paragraph("Idiomas", section_st)); story.append(hr())
        story.append(Paragraph("  •  ".join(cv["idiomas"]), body_st))

    doc.build(story)
    return buffer.getvalue()
```

- [ ] **Step 4: Run tests — expect PASS**

```bash
pytest tests/test_cv_generator.py -v
```

- [ ] **Step 5: Commit**

```bash
git add cv_generator.py tests/test_cv_generator.py
git commit -m "feat: add CV generator with Groq improvement and reportlab PDF"
```

---

## Task 6: main.py

**Files:** `main.py`

- [ ] **Step 1: Create main.py**

```python
import os, json, time
from pathlib import Path
from fastapi import FastAPI, File, UploadFile, Form, HTTPException, Depends, Body, Header
from fastapi.responses import StreamingResponse, FileResponse
from dotenv import load_dotenv

load_dotenv()

from database import init_db, insert_candidate, get_all_candidates, get_stats, get_candidate, mark_email_sent
from analyzer import analyze_cv
from email_sender import send_feedback_email
from cv_generator import improve_cv_with_ai, generate_cv_pdf

app = FastAPI(title="CVSmart V2")

CVS_DIR = Path("cvs")
CVS_DIR.mkdir(exist_ok=True)
init_db()

# ── Auth ──────────────────────────────────────────────────────────────
def require_auth(x_recruiter_password: str = Header(None)):
    if x_recruiter_password != os.getenv("SECRET_PANEL", "cvsmart2026"):
        raise HTTPException(status_code=401, detail="Contraseña incorrecta")

# ── Candidatos ────────────────────────────────────────────────────────
@app.post("/api/aplicar")
async def aplicar(
    cv: UploadFile = File(...),
    email: str = Form(...),
    phone: str = Form(""),
    availability: str = Form(...),
    expected_salary: str = Form(...),
    specific_experience: str = Form(...),
):
    if cv.content_type != "application/pdf":
        raise HTTPException(400, "Solo se aceptan archivos PDF")

    pdf_bytes = await cv.read()

    try:
        analysis = analyze_cv(pdf_bytes)
    except ValueError as e:
        raise HTTPException(422, str(e))
    except Exception as e:
        raise HTTPException(503, f"Error al analizar CV: {e}")

    filename = f"cv-{int(time.time())}-{cv.filename}"
    (CVS_DIR / filename).write_bytes(pdf_bytes)

    cid = insert_candidate(
        name=analysis.get("nombre", "Sin nombre"),
        email=email, phone=phone,
        cv_filename=filename, cv_original_name=cv.filename,
        score=analysis.get("puntaje", 5),
        score_label=analysis.get("score_label", "Bueno"),
        years_experience=analysis.get("años_experiencia", 0),
        education_level=analysis.get("nivel_estudios", ""),
        matching_skills=json.dumps(analysis.get("habilidades_coincidentes", [])),
        summary=analysis.get("resumen", ""),
        strength=analysis.get("fortaleza", ""),
        weaknesses=json.dumps(analysis.get("debilidades", [])),
        full_analysis=json.dumps(analysis),
        availability=availability,
        expected_salary=expected_salary,
        specific_experience=specific_experience,
    )

    if email:
        sent = send_feedback_email(
            to_email=email,
            name=analysis.get("nombre", "Candidato"),
            score_label=analysis.get("score_label", "Bueno"),
            strength=analysis.get("fortaleza", ""),
            weaknesses=analysis.get("debilidades", []),
        )
        if sent:
            mark_email_sent(cid)

    return {
        "success": True,
        "candidateId": cid,
        "name": analysis.get("nombre"),
        "message": "Tu postulación fue recibida. Recibirás un correo con retroalimentación.",
    }

# ── Panel reclutador ──────────────────────────────────────────────────
@app.get("/api/panel/candidatos")
def panel_candidatos(_=Depends(require_auth)):
    return {"candidates": get_all_candidates()}

@app.get("/api/panel/stats")
def panel_stats(_=Depends(require_auth)):
    return get_stats()

@app.get("/api/panel/candidatos/{cid}")
def panel_detail(cid: int, _=Depends(require_auth)):
    c = get_candidate(cid)
    if not c:
        raise HTTPException(404, "Candidato no encontrado")
    return c

@app.get("/api/panel/candidatos/{cid}/cv")
def panel_cv(cid: int, _=Depends(require_auth)):
    c = get_candidate(cid)
    if not c:
        raise HTTPException(404, "Candidato no encontrado")
    path = CVS_DIR / c["cv_filename"]
    if not path.exists():
        raise HTTPException(404, "Archivo no encontrado")
    return FileResponse(path, filename=c.get("cv_original_name", "cv.pdf"))

# ── Generador de CV ───────────────────────────────────────────────────
@app.post("/api/generar-cv")
async def generar_cv(cv_data: dict = Body(...)):
    try:
        improved = improve_cv_with_ai(cv_data)
    except Exception as e:
        raise HTTPException(503, f"Error al generar CV: {e}")
    pdf_bytes = generate_cv_pdf(improved)
    name = improved.get("nombre", "CV").replace(" ", "_")
    return StreamingResponse(
        iter([pdf_bytes]),
        media_type="application/pdf",
        headers={"Content-Disposition": f'attachment; filename="CV-{name}.pdf"'},
    )

# ── Páginas HTML ──────────────────────────────────────────────────────
@app.get("/", include_in_schema=False)
def serve_index(): return FileResponse("frontend/index.html")

@app.get("/aplicar", include_in_schema=False)
def serve_aplicar(): return FileResponse("frontend/aplicar.html")

@app.get("/panel", include_in_schema=False)
def serve_panel(): return FileResponse("frontend/panel.html")

@app.get("/crear-cv", include_in_schema=False)
def serve_crear_cv(): return FileResponse("frontend/crear-cv.html")
```

- [ ] **Step 2: Verify app starts**

```bash
uvicorn main:app --port 8000
```

Expected: `Application startup complete.` with no import errors. Stop with Ctrl+C.

- [ ] **Step 3: Commit**

```bash
git add main.py
git commit -m "feat: add FastAPI main with all routes"
```

---

## Task 7: frontend/index.html

**Files:** `frontend/index.html`

- [ ] **Step 1: Create frontend/index.html**

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>CVSmart — Filtrado Inteligente de CVs con IA</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root {
      --navy: #1F3864; --blue: #2E75B6; --accent: #4FC3F7;
      --bg: #F8FAFF; --white: #ffffff; --gray: #64748b; --border: rgba(255,255,255,.3);
    }
    body { font-family: 'Inter', sans-serif; background: var(--bg); color: #1e293b; }

    /* NAV */
    nav { position: fixed; top: 0; left: 0; right: 0; z-index: 100;
          background: rgba(255,255,255,.9); backdrop-filter: blur(12px);
          border-bottom: 1px solid rgba(31,56,100,.08);
          display: flex; align-items: center; justify-content: space-between;
          padding: 0 2rem; height: 64px; }
    .nav-logo { font-weight: 800; font-size: 1.25rem; color: var(--navy); text-decoration: none; }
    .nav-links { display: flex; gap: 1rem; align-items: center; }
    .nav-links a { text-decoration: none; color: var(--gray); font-weight: 500; transition: color .2s; }
    .nav-links a:hover { color: var(--navy); }
    .nav-links .btn-nav { background: linear-gradient(135deg, var(--navy), var(--blue));
                          color: #fff !important; padding: 8px 20px; border-radius: 10px; }

    /* HERO */
    .hero { min-height: 100vh; display: flex; flex-direction: column; align-items: center;
            justify-content: center; text-align: center; padding: 120px 2rem 4rem;
            background: linear-gradient(135deg, #0f1f3d 0%, #1F3864 50%, #2E75B6 100%); }
    .hero h1 { font-size: clamp(2.5rem, 6vw, 4.5rem); font-weight: 800; color: #fff;
               line-height: 1.1; margin-bottom: 1.5rem; }
    .hero h1 span { color: var(--accent); }
    .hero p { font-size: 1.2rem; color: rgba(255,255,255,.8); max-width: 600px; margin-bottom: 2.5rem; }
    .hero-btns { display: flex; gap: 1rem; flex-wrap: wrap; justify-content: center; }
    .btn { display: inline-flex; align-items: center; gap: 8px; padding: 14px 32px;
           border-radius: 12px; font-weight: 600; font-size: 1rem; text-decoration: none;
           transition: transform .2s, box-shadow .2s; }
    .btn:hover { transform: translateY(-2px); }
    .btn-white { background: #fff; color: var(--navy); box-shadow: 0 4px 20px rgba(0,0,0,.15); }
    .btn-white:hover { box-shadow: 0 8px 32px rgba(0,0,0,.2); }
    .btn-outline { border: 2px solid rgba(255,255,255,.5); color: #fff; }
    .btn-outline:hover { background: rgba(255,255,255,.1); }
    .btn-primary { background: linear-gradient(135deg, var(--navy), var(--blue)); color: #fff;
                   box-shadow: 0 4px 20px rgba(31,56,100,.3); }
    .btn-primary:hover { box-shadow: 0 8px 32px rgba(31,56,100,.4); }

    /* SECCIONES */
    section { padding: 5rem 2rem; }
    .container { max-width: 1100px; margin: 0 auto; }
    .section-title { font-size: 2rem; font-weight: 800; color: var(--navy); text-align: center; margin-bottom: .75rem; }
    .section-sub { text-align: center; color: var(--gray); font-size: 1.1rem; margin-bottom: 3rem; }

    /* GLASS CARD */
    .glass { background: rgba(255,255,255,.85); backdrop-filter: blur(12px);
             border: 1px solid rgba(255,255,255,.3); border-radius: 20px;
             box-shadow: 0 8px 32px rgba(31,56,100,.12); padding: 2rem; }

    /* HOW IT WORKS */
    .steps { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 1.5rem; }
    .step { text-align: center; }
    .step-num { width: 60px; height: 60px; background: linear-gradient(135deg, var(--navy), var(--blue));
                color: #fff; border-radius: 50%; display: flex; align-items: center; justify-content: center;
                font-size: 1.5rem; font-weight: 800; margin: 0 auto 1rem; }
    .step h3 { font-size: 1.1rem; color: var(--navy); margin-bottom: .5rem; }
    .step p { color: var(--gray); font-size: .95rem; }

    /* FEATURES */
    .features { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1.5rem; }
    .feature { text-align: center; }
    .feature-icon { font-size: 2.5rem; margin-bottom: 1rem; }
    .feature h3 { font-size: 1.1rem; color: var(--navy); margin-bottom: .5rem; }
    .feature p { color: var(--gray); font-size: .9rem; }

    /* FOOTER */
    footer { background: var(--navy); color: rgba(255,255,255,.6); text-align: center; padding: 2rem; font-size: .9rem; }
    footer strong { color: var(--accent); }

    /* FADE IN */
    .fade-in { opacity: 0; transform: translateY(24px); transition: opacity .6s ease, transform .6s ease; }
    .fade-in.visible { opacity: 1; transform: translateY(0); }

    @media (max-width: 640px) {
      .nav-links a:not(.btn-nav) { display: none; }
    }
  </style>
</head>
<body>

<nav>
  <a class="nav-logo" href="/">🧠 CVSmart</a>
  <div class="nav-links">
    <a href="/">Inicio</a>
    <a href="/aplicar">Postularme</a>
    <a href="/crear-cv">Crear CV</a>
    <a href="/panel">Reclutador</a>
    <a href="/aplicar" class="btn-nav">Subir CV →</a>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <h1>Filtra CVs con<br><span>Inteligencia Artificial</span></h1>
  <p>CVSmart analiza cada candidato, lo puntúa del 1 al 10 y entrega al reclutador una lista priorizada lista para tomar decisiones.</p>
  <div class="hero-btns">
    <a href="/aplicar" class="btn btn-white">📄 Subir mi CV</a>
    <a href="/panel" class="btn btn-outline">🔒 Panel Reclutador</a>
  </div>
</section>

<!-- CÓMO FUNCIONA -->
<section style="background: var(--white);">
  <div class="container">
    <h2 class="section-title fade-in">¿Cómo funciona?</h2>
    <p class="section-sub fade-in">Tres pasos del CV al ranking</p>
    <div class="steps">
      <div class="glass step fade-in">
        <div class="step-num">1</div>
        <h3>El candidato sube su CV</h3>
        <p>Sube su PDF en el formulario web y responde las 3 preguntas de filtro en segundos.</p>
      </div>
      <div class="glass step fade-in">
        <div class="step-num">2</div>
        <h3>IA analiza el perfil</h3>
        <p>Groq extrae el texto, evalúa el perfil y genera un puntaje del 1 al 10 con fortalezas y áreas de mejora.</p>
      </div>
      <div class="glass step fade-in">
        <div class="step-num">3</div>
        <h3>Reclutador decide</h3>
        <p>Ve el ranking, revisa resúmenes y descarga los CVs de los mejores perfiles desde el panel.</p>
      </div>
    </div>
  </div>
</section>

<!-- BENEFICIOS -->
<section>
  <div class="container">
    <h2 class="section-title fade-in">Todo lo que necesitas</h2>
    <p class="section-sub fade-in">Una plataforma completa para reclutamiento inteligente</p>
    <div class="features">
      <div class="glass feature fade-in">
        <div class="feature-icon">📤</div>
        <h3>Recepción por formulario web</h3>
        <p>Los candidatos suben su CV directamente desde esta plataforma. Sin correos ni carpetas desordenadas.</p>
      </div>
      <div class="glass feature fade-in">
        <div class="feature-icon">🤖</div>
        <h3>Análisis con IA</h3>
        <p>Groq analiza cada CV evaluando experiencia, estudios y habilidades relevantes para el puesto.</p>
      </div>
      <div class="glass feature fade-in">
        <div class="feature-icon">📧</div>
        <h3>Retroalimentación automática</h3>
        <p>Cada candidato recibe un correo con su nivel de perfil, fortaleza y áreas de oportunidad.</p>
      </div>
      <div class="glass feature fade-in">
        <div class="feature-icon">📝</div>
        <h3>Creador de CV con IA</h3>
        <p>Los candidatos pueden generar un CV profesional con ayuda de IA y descargarlo como PDF.</p>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<section style="background: linear-gradient(135deg, var(--navy), var(--blue)); text-align: center;">
  <h2 style="color:#fff; font-size: 2rem; font-weight: 800; margin-bottom: 1rem;">¿Listo para empezar?</h2>
  <p style="color: rgba(255,255,255,.8); margin-bottom: 2rem;">Sube tu CV ahora y recibe retroalimentación personalizada con IA.</p>
  <a href="/aplicar" class="btn btn-white">📄 Subir mi CV ahora</a>
</section>

<footer>
  <p>CVSmart &copy; 2026 — Potenciado por <strong>Groq AI</strong></p>
</footer>

<script>
  const observer = new IntersectionObserver(entries => {
    entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
  }, { threshold: 0.1 });
  document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));
</script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add frontend/index.html
git commit -m "feat: add redesigned landing page"
```

---

## Task 8: frontend/aplicar.html

**Files:** `frontend/aplicar.html`

- [ ] **Step 1: Create frontend/aplicar.html**

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Postularme — CVSmart</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root { --navy: #1F3864; --blue: #2E75B6; --accent: #4FC3F7; --bg: #F8FAFF; --gray: #64748b; }
    body { font-family: 'Inter', sans-serif; background: var(--bg); color: #1e293b; min-height: 100vh; }
    nav { position: fixed; top: 0; left: 0; right: 0; z-index: 100;
          background: rgba(255,255,255,.9); backdrop-filter: blur(12px);
          border-bottom: 1px solid rgba(31,56,100,.08);
          display: flex; align-items: center; justify-content: space-between;
          padding: 0 2rem; height: 64px; }
    .nav-logo { font-weight: 800; font-size: 1.25rem; color: var(--navy); text-decoration: none; }
    .nav-links { display: flex; gap: 1rem; align-items: center; }
    .nav-links a { text-decoration: none; color: var(--gray); font-weight: 500; }
    .nav-links .active { color: var(--navy); font-weight: 700; }

    main { padding-top: 96px; padding-bottom: 4rem; display: flex; justify-content: center; }
    .card { background: rgba(255,255,255,.85); backdrop-filter: blur(12px);
            border: 1px solid rgba(255,255,255,.3); border-radius: 20px;
            box-shadow: 0 8px 32px rgba(31,56,100,.12);
            padding: 2.5rem; width: 100%; max-width: 680px; margin: 0 1rem; }
    h1 { font-size: 1.8rem; font-weight: 800; color: var(--navy); margin-bottom: .5rem; }
    .subtitle { color: var(--gray); margin-bottom: 2rem; }
    label { display: block; font-weight: 600; font-size: .9rem; color: var(--navy); margin-bottom: 6px; }
    .req { color: #ef4444; }
    input, select, textarea {
      width: 100%; padding: 12px 16px; border: 1.5px solid #e2e8f0;
      border-radius: 10px; font-size: .95rem; font-family: inherit;
      transition: border-color .2s, box-shadow .2s; background: #fff; color: #1e293b;
    }
    input:focus, select:focus, textarea:focus {
      outline: none; border-color: var(--blue);
      box-shadow: 0 0 0 3px rgba(46,117,182,.15);
    }
    textarea { resize: vertical; min-height: 100px; }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
    .form-group { margin-bottom: 1.25rem; }
    .divider { border: none; border-top: 1px solid #e2e8f0; margin: 1.5rem 0; }
    .section-label { font-weight: 700; font-size: .8rem; text-transform: uppercase;
                     letter-spacing: .08em; color: var(--blue); margin-bottom: 1rem; }

    /* Upload */
    .upload-zone { border: 2px dashed #cbd5e1; border-radius: 14px; padding: 2rem;
                   text-align: center; cursor: pointer; transition: all .2s; background: #f8fafc; }
    .upload-zone:hover, .upload-zone.drag-over { border-color: var(--blue); background: #eff6ff; }
    .upload-zone input { display: none; }
    .upload-icon { font-size: 2.5rem; margin-bottom: .75rem; }
    .upload-zone p { color: var(--gray); font-size: .9rem; }
    .upload-zone strong { color: var(--navy); }
    .file-selected { display: none; align-items: center; gap: 10px; padding: 12px 16px;
                     background: #f0fdf4; border: 1.5px solid #86efac; border-radius: 10px;
                     color: #166534; font-weight: 600; }

    /* Progress */
    .progress-wrap { margin-top: 1rem; display: none; }
    .progress-bar { height: 6px; background: #e2e8f0; border-radius: 99px; overflow: hidden; }
    .progress-fill { height: 100%; background: linear-gradient(90deg, var(--navy), var(--accent));
                     border-radius: 99px; width: 0; transition: width .3s ease; }

    /* Button */
    .btn-submit { width: 100%; padding: 14px; background: linear-gradient(135deg, var(--navy), var(--blue));
                  color: #fff; border: none; border-radius: 12px; font-size: 1rem; font-weight: 700;
                  cursor: pointer; transition: transform .2s, box-shadow .2s; margin-top: .5rem; }
    .btn-submit:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(31,56,100,.3); }
    .btn-submit:disabled { opacity: .6; cursor: not-allowed; transform: none; }

    /* Loading */
    .loading { display: none; text-align: center; padding: 3rem 0; }
    .spinner { width: 48px; height: 48px; border: 4px solid #e2e8f0;
               border-top-color: var(--blue); border-radius: 50%;
               animation: spin 1s linear infinite; margin: 0 auto 1rem; }
    @keyframes spin { to { transform: rotate(360deg); } }
    .loading p { color: var(--gray); font-size: .95rem; }

    /* Success */
    .success { display: none; text-align: center; padding: 2rem 0; }
    .success-icon { font-size: 4rem; margin-bottom: 1rem; }
    .success h2 { color: var(--navy); font-size: 1.5rem; margin-bottom: .75rem; }
    .success p { color: var(--gray); margin-bottom: 2rem; line-height: 1.6; }
    .btn-back { display: inline-block; padding: 12px 28px; border: 2px solid var(--blue);
                color: var(--blue); border-radius: 10px; text-decoration: none; font-weight: 600; }

    /* Alert */
    .alert { padding: 12px 16px; border-radius: 10px; margin-bottom: 1rem;
             display: flex; align-items: flex-start; gap: 10px; font-size: .9rem; }
    .alert-error { background: #fef2f2; color: #b91c1c; border: 1px solid #fecaca; }

    @media (max-width: 540px) { .form-row { grid-template-columns: 1fr; } }
  </style>
</head>
<body>
<nav>
  <a class="nav-logo" href="/">🧠 CVSmart</a>
  <div class="nav-links">
    <a href="/">Inicio</a>
    <a href="/aplicar" class="active">Postularme</a>
    <a href="/crear-cv">Crear CV</a>
    <a href="/panel">Reclutador</a>
  </div>
</nav>

<main>
  <div class="card">
    <h1>Postula ahora</h1>
    <p class="subtitle">Sube tu CV en PDF y responde las preguntas. El análisis tarda menos de 30 segundos.</p>

    <div id="alertBox"></div>

    <!-- FORM -->
    <form id="applyForm" novalidate>
      <p class="section-label">Datos de contacto</p>
      <div class="form-row">
        <div class="form-group">
          <label>Email <span class="req">*</span></label>
          <input type="email" name="email" id="email" placeholder="tu@correo.com" required />
        </div>
        <div class="form-group">
          <label>Teléfono</label>
          <input type="tel" name="phone" placeholder="+52 55 0000 0000" />
        </div>
      </div>

      <hr class="divider" />
      <p class="section-label">Curriculum Vitae</p>
      <div class="form-group">
        <div class="upload-zone" id="uploadZone">
          <input type="file" id="cvFile" name="cv" accept=".pdf,application/pdf" />
          <div class="upload-icon">📄</div>
          <p><strong>Haz clic aquí</strong> o arrastra tu PDF</p>
          <p style="font-size:.8rem; margin-top:4px; color:#94a3b8">Máximo 10 MB · Solo PDF</p>
        </div>
        <div class="file-selected" id="fileSelected">
          <span>✅</span><span id="fileName"></span>
        </div>
        <div class="progress-wrap" id="progressWrap">
          <div class="progress-bar"><div class="progress-fill" id="progressFill"></div></div>
          <p style="font-size:.8rem; color:var(--gray); margin-top:6px" id="progressLabel">Subiendo...</p>
        </div>
      </div>

      <hr class="divider" />
      <p class="section-label">Preguntas de filtro</p>
      <div class="form-group">
        <label>¿Cuál es tu disponibilidad? <span class="req">*</span></label>
        <select name="availability" id="availability" required>
          <option value="">Selecciona una opción</option>
          <option value="Inmediata">Inmediata</option>
          <option value="2 semanas">2 semanas</option>
          <option value="1 mes">1 mes</option>
        </select>
      </div>
      <div class="form-group">
        <label>¿Cuál es tu expectativa de sueldo mensual en MXN? <span class="req">*</span></label>
        <input type="text" name="expected_salary" id="salary" placeholder="Ej: $25,000 MXN" required />
      </div>
      <div class="form-group">
        <label>¿Cuántos años de experiencia tienes en el área? <span class="req">*</span></label>
        <input type="number" name="specific_experience" id="experience" min="0" max="50" placeholder="Ej: 3" required />
      </div>

      <button type="submit" class="btn-submit" id="submitBtn">🚀 Enviar postulación</button>
    </form>

    <!-- LOADING -->
    <div class="loading" id="loadingState">
      <div class="spinner"></div>
      <p id="loadingMsg">Analizando tu CV con IA...</p>
    </div>

    <!-- SUCCESS -->
    <div class="success" id="successState">
      <div class="success-icon">🎉</div>
      <h2>¡Postulación recibida!</h2>
      <p>Tu CV fue analizado y guardado exitosamente.<br>
         <strong>Revisa tu correo</strong> — te enviamos retroalimentación personalizada con tus fortalezas y áreas de mejora.</p>
      <div style="display:flex; gap:1rem; justify-content:center; flex-wrap:wrap;">
        <a href="/" class="btn-back">← Volver al inicio</a>
        <a href="/crear-cv" style="display:inline-block; padding:12px 28px; background:linear-gradient(135deg,var(--navy),var(--blue)); color:#fff; border-radius:10px; text-decoration:none; font-weight:600;">Mejorar mi CV →</a>
      </div>
    </div>
  </div>
</main>

<script>
  const uploadZone = document.getElementById('uploadZone');
  const cvFile     = document.getElementById('cvFile');
  const fileSelected = document.getElementById('fileSelected');
  const fileName   = document.getElementById('fileName');

  uploadZone.addEventListener('click', () => cvFile.click());
  uploadZone.addEventListener('dragover', e => { e.preventDefault(); uploadZone.classList.add('drag-over'); });
  uploadZone.addEventListener('dragleave', () => uploadZone.classList.remove('drag-over'));
  uploadZone.addEventListener('drop', e => {
    e.preventDefault(); uploadZone.classList.remove('drag-over');
    const f = e.dataTransfer.files[0];
    if (f?.type === 'application/pdf') setFile(f);
    else showAlert('Solo se aceptan archivos PDF.');
  });
  cvFile.addEventListener('change', () => { if (cvFile.files[0]) setFile(cvFile.files[0]); });

  function setFile(f) {
    const dt = new DataTransfer(); dt.items.add(f); cvFile.files = dt.files;
    fileName.textContent = f.name;
    fileSelected.style.display = 'flex';
    uploadZone.style.display = 'none';
  }

  function showAlert(msg) {
    document.getElementById('alertBox').innerHTML =
      `<div class="alert alert-error"><span>❌</span><span>${msg}</span></div>`;
  }

  function animateProgress() {
    const fill = document.getElementById('progressFill');
    const label = document.getElementById('progressLabel');
    const msgs = ['Subiendo archivo...', 'Extrayendo texto...', 'Analizando con IA...', 'Calculando puntaje...'];
    let pct = 0, step = 0;
    document.getElementById('progressWrap').style.display = 'block';
    return setInterval(() => {
      pct = Math.min(pct + Math.random() * 15, 90);
      fill.style.width = pct + '%';
      label.textContent = msgs[Math.min(step++, msgs.length - 1)];
    }, 1200);
  }

  document.getElementById('applyForm').addEventListener('submit', async e => {
    e.preventDefault();
    const email = document.getElementById('email').value.trim();
    const avail = document.getElementById('availability').value;
    const salary = document.getElementById('salary').value.trim();
    const exp   = document.getElementById('experience').value.trim();
    if (!email)  return showAlert('El email es obligatorio.');
    if (!cvFile.files[0]) return showAlert('Selecciona un archivo PDF.');
    if (!avail || !salary || !exp) return showAlert('Responde todas las preguntas de filtro.');

    document.getElementById('applyForm').style.display = 'none';
    document.getElementById('loadingState').style.display = 'block';
    const timer = animateProgress();

    try {
      const fd = new FormData(e.target);
      const res = await fetch('/api/aplicar', { method: 'POST', body: fd });
      clearInterval(timer);
      document.getElementById('progressFill').style.width = '100%';
      const data = await res.json();
      if (!res.ok) {
        document.getElementById('loadingState').style.display = 'none';
        document.getElementById('applyForm').style.display = 'block';
        return showAlert(data.detail || 'Error al procesar la postulación.');
      }
      document.getElementById('loadingState').style.display = 'none';
      document.getElementById('successState').style.display = 'block';
    } catch {
      clearInterval(timer);
      document.getElementById('loadingState').style.display = 'none';
      document.getElementById('applyForm').style.display = 'block';
      showAlert('Error de conexión. Verifica que el servidor esté corriendo.');
    }
  });
</script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add frontend/aplicar.html
git commit -m "feat: add candidate application form"
```

---

## Task 9: frontend/panel.html

**Files:** `frontend/panel.html`

- [ ] **Step 1: Create frontend/panel.html**

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Panel Reclutador — CVSmart</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root { --navy: #1F3864; --blue: #2E75B6; --accent: #4FC3F7; --bg: #F8FAFF; --gray: #64748b; }
    body { font-family: 'Inter', sans-serif; background: var(--bg); color: #1e293b; }
    nav { position: fixed; top: 0; left: 0; right: 0; z-index: 100;
          background: rgba(255,255,255,.9); backdrop-filter: blur(12px);
          border-bottom: 1px solid rgba(31,56,100,.08);
          display: flex; align-items: center; justify-content: space-between;
          padding: 0 2rem; height: 64px; }
    .nav-logo { font-weight: 800; font-size: 1.25rem; color: var(--navy); text-decoration: none; }
    .nav-links { display: flex; gap: 1rem; }
    .nav-links a { text-decoration: none; color: var(--gray); font-weight: 500; }

    main { padding-top: 80px; max-width: 1200px; margin: 0 auto; padding-left: 1.5rem; padding-right: 1.5rem; padding-bottom: 4rem; }

    /* LOGIN */
    .login-wrap { min-height: calc(100vh - 80px); display: flex; align-items: center; justify-content: center; }
    .login-card { background: rgba(255,255,255,.85); backdrop-filter: blur(12px);
                  border-radius: 20px; box-shadow: 0 8px 32px rgba(31,56,100,.12);
                  padding: 2.5rem; width: 100%; max-width: 400px; text-align: center; }
    .login-card h2 { color: var(--navy); font-size: 1.5rem; margin-bottom: .5rem; }
    .login-card p { color: var(--gray); margin-bottom: 1.5rem; }
    input[type=password] { width: 100%; padding: 12px 16px; border: 1.5px solid #e2e8f0;
                           border-radius: 10px; font-size: 1rem; font-family: inherit; margin-bottom: 1rem;
                           transition: border-color .2s, box-shadow .2s; }
    input[type=password]:focus { outline: none; border-color: var(--blue); box-shadow: 0 0 0 3px rgba(46,117,182,.15); }
    .btn-primary { width: 100%; padding: 13px; background: linear-gradient(135deg, var(--navy), var(--blue));
                   color: #fff; border: none; border-radius: 12px; font-size: 1rem; font-weight: 700;
                   cursor: pointer; transition: transform .2s, box-shadow .2s; }
    .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(31,56,100,.3); }
    .error-msg { color: #ef4444; font-size: .9rem; margin-top: .5rem; }

    /* DASHBOARD */
    #dashboard { display: none; }
    .page-header { padding: 2rem 0 1.5rem; }
    .page-header h1 { font-size: 1.8rem; font-weight: 800; color: var(--navy); }
    .page-header p { color: var(--gray); }

    .stats { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 1rem; margin-bottom: 2rem; }
    .stat-card { background: rgba(255,255,255,.85); backdrop-filter: blur(12px);
                 border-radius: 16px; box-shadow: 0 4px 16px rgba(31,56,100,.08);
                 padding: 1.5rem; text-align: center; }
    .stat-val { font-size: 2.5rem; font-weight: 800; color: var(--navy); }
    .stat-label { font-size: .85rem; color: var(--gray); margin-top: 4px; }

    /* TABLE */
    .table-wrap { background: rgba(255,255,255,.85); backdrop-filter: blur(12px);
                  border-radius: 20px; box-shadow: 0 8px 32px rgba(31,56,100,.12); overflow: hidden; }
    table { width: 100%; border-collapse: collapse; }
    thead th { background: var(--navy); color: #fff; padding: 14px 16px; text-align: left; font-size: .85rem; font-weight: 600; }
    tbody tr { border-bottom: 1px solid #f1f5f9; transition: background .15s; }
    tbody tr:hover { background: #f8faff; }
    tbody td { padding: 14px 16px; font-size: .9rem; vertical-align: middle; }
    .avatar { width: 38px; height: 38px; border-radius: 50%; background: linear-gradient(135deg, var(--navy), var(--blue));
              color: #fff; display: flex; align-items: center; justify-content: center;
              font-weight: 700; font-size: .85rem; flex-shrink: 0; }
    .name-cell { display: flex; align-items: center; gap: 10px; }
    .badge { padding: 4px 12px; border-radius: 99px; font-size: .8rem; font-weight: 700; }
    .badge-green  { background: #dcfce7; color: #166534; }
    .badge-yellow { background: #fef3c7; color: #92400e; }
    .badge-red    { background: #fee2e2; color: #991b1b; }
    .btn-detail { padding: 6px 14px; background: var(--blue); color: #fff; border: none;
                  border-radius: 8px; font-size: .8rem; font-weight: 600; cursor: pointer; transition: opacity .2s; }
    .btn-detail:hover { opacity: .85; }
    .empty { text-align: center; padding: 3rem; color: var(--gray); }

    /* MODAL */
    .modal-overlay { display: none; position: fixed; inset: 0; background: rgba(0,0,0,.5);
                     backdrop-filter: blur(4px); z-index: 1000; align-items: center; justify-content: center; padding: 1rem; }
    .modal-overlay.open { display: flex; }
    .modal { background: #fff; border-radius: 20px; box-shadow: 0 24px 64px rgba(0,0,0,.2);
             padding: 2rem; width: 100%; max-width: 620px; max-height: 85vh; overflow-y: auto; }
    .modal-header { display: flex; align-items: center; gap: 14px; margin-bottom: 1.5rem; }
    .modal-avatar { width: 56px; height: 56px; border-radius: 50%; background: linear-gradient(135deg, var(--navy), var(--blue));
                    color: #fff; display: flex; align-items: center; justify-content: center; font-size: 1.2rem; font-weight: 800; }
    .modal-title h2 { font-size: 1.3rem; color: var(--navy); }
    .modal-title p { color: var(--gray); font-size: .9rem; }
    .modal-section { margin-bottom: 1.25rem; }
    .modal-section h4 { font-size: .8rem; text-transform: uppercase; letter-spacing: .06em;
                        color: var(--blue); font-weight: 700; margin-bottom: 8px; }
    .modal-section p { color: #374151; font-size: .9rem; line-height: 1.6; }
    .skills-list { display: flex; flex-wrap: wrap; gap: 6px; }
    .skill-tag { padding: 4px 12px; background: #eff6ff; color: var(--blue); border-radius: 99px; font-size: .8rem; font-weight: 600; }
    .modal-footer { display: flex; gap: 1rem; margin-top: 1.5rem; flex-wrap: wrap; }
    .btn-close { flex: 1; padding: 12px; border: 2px solid #e2e8f0; border-radius: 10px; background: none;
                 cursor: pointer; font-weight: 600; color: var(--gray); }
    .btn-download { flex: 2; padding: 12px; background: linear-gradient(135deg, var(--navy), var(--blue));
                    color: #fff; border: none; border-radius: 10px; cursor: pointer; font-weight: 700; font-size: .95rem; }
    .answer-row { display: flex; justify-content: space-between; padding: 8px 0;
                  border-bottom: 1px solid #f1f5f9; font-size: .9rem; }
    .answer-row:last-child { border-bottom: none; }
    .answer-row span:first-child { color: var(--gray); }
    .answer-row span:last-child { font-weight: 600; color: #1e293b; }
  </style>
</head>
<body>
<nav>
  <a class="nav-logo" href="/">🧠 CVSmart</a>
  <div class="nav-links">
    <a href="/">Inicio</a>
    <a href="/panel">Panel</a>
  </div>
</nav>

<main>
  <!-- LOGIN -->
  <div class="login-wrap" id="loginWrap">
    <div class="login-card">
      <div style="font-size:2.5rem; margin-bottom:1rem">🔒</div>
      <h2>Panel Reclutador</h2>
      <p>Ingresa la contraseña para acceder al dashboard</p>
      <input type="password" id="passwordInput" placeholder="Contraseña" />
      <button class="btn-primary" onclick="doLogin()">Acceder al panel</button>
      <p class="error-msg" id="loginError"></p>
    </div>
  </div>

  <!-- DASHBOARD -->
  <div id="dashboard">
    <div class="page-header">
      <h1>Panel de Reclutamiento</h1>
      <p>Candidatos recibidos y analizados por IA</p>
    </div>
    <div class="stats" id="statsRow">
      <div class="stat-card"><div class="stat-val" id="statTotal">—</div><div class="stat-label">Total candidatos</div></div>
      <div class="stat-card"><div class="stat-val" id="statAvg">—</div><div class="stat-label">Puntaje promedio</div></div>
      <div class="stat-card"><div class="stat-val" id="statTop">—</div><div class="stat-label">Top candidatos (8+)</div></div>
    </div>
    <div class="table-wrap">
      <table>
        <thead>
          <tr>
            <th>Candidato</th><th>Puntaje</th><th>Nivel</th>
            <th>Experiencia</th><th>Disponibilidad</th><th>Fecha</th><th></th>
          </tr>
        </thead>
        <tbody id="tableBody"></tbody>
      </table>
    </div>
  </div>
</main>

<!-- MODAL -->
<div class="modal-overlay" id="modalOverlay">
  <div class="modal" id="modal">
    <div class="modal-header">
      <div class="modal-avatar" id="mAvatar"></div>
      <div class="modal-title">
        <h2 id="mName"></h2>
        <p id="mEmail"></p>
      </div>
    </div>
    <div class="modal-section">
      <h4>Resumen ejecutivo</h4>
      <p id="mSummary"></p>
    </div>
    <div class="modal-section">
      <h4>Fortaleza principal</h4>
      <p id="mStrength" style="color:#166534; background:#dcfce7; padding:10px 14px; border-radius:8px;"></p>
    </div>
    <div class="modal-section">
      <h4>Áreas de oportunidad</h4>
      <ul id="mWeaknesses" style="padding-left:1.2rem; color:#374151; font-size:.9rem; line-height:1.8;"></ul>
    </div>
    <div class="modal-section">
      <h4>Habilidades coincidentes</h4>
      <div class="skills-list" id="mSkills"></div>
    </div>
    <div class="modal-section">
      <h4>Respuestas de filtro</h4>
      <div id="mAnswers"></div>
    </div>
    <div class="modal-footer">
      <button class="btn-close" onclick="closeModal()">Cerrar</button>
      <button class="btn-download" id="mDownloadBtn">⬇️ Descargar CV</button>
    </div>
  </div>
</div>

<script>
  let AUTH = '';

  async function doLogin() {
    const pw = document.getElementById('passwordInput').value;
    const res = await fetch('/api/panel/stats', { headers: { 'X-Recruiter-Password': pw } });
    if (res.status === 401) {
      document.getElementById('loginError').textContent = 'Contraseña incorrecta.';
      return;
    }
    AUTH = pw;
    document.getElementById('loginWrap').style.display = 'none';
    document.getElementById('dashboard').style.display = 'block';
    loadDashboard();
  }

  document.getElementById('passwordInput').addEventListener('keydown', e => { if (e.key === 'Enter') doLogin(); });

  async function loadDashboard() {
    const [statsRes, candsRes] = await Promise.all([
      fetch('/api/panel/stats', { headers: { 'X-Recruiter-Password': AUTH } }),
      fetch('/api/panel/candidatos', { headers: { 'X-Recruiter-Password': AUTH } }),
    ]);
    const stats = await statsRes.json();
    const { candidates } = await candsRes.json();

    document.getElementById('statTotal').textContent = stats.total ?? 0;
    document.getElementById('statAvg').textContent = stats.avg_score ?? '—';
    document.getElementById('statTop').textContent = stats.high_score ?? 0;

    const tbody = document.getElementById('tableBody');
    if (!candidates.length) {
      tbody.innerHTML = '<tr><td colspan="7" class="empty">No hay candidatos aún</td></tr>';
      return;
    }
    tbody.innerHTML = candidates.map(c => {
      const initials = c.name.split(' ').map(w => w[0]).slice(0, 2).join('').toUpperCase();
      const badgeCls = c.score >= 8 ? 'badge-green' : c.score >= 5 ? 'badge-yellow' : 'badge-red';
      const date = new Date(c.created_at).toLocaleDateString('es-MX', { day:'2-digit', month:'short' });
      return `<tr>
        <td><div class="name-cell"><div class="avatar">${initials}</div>${c.name}</div></td>
        <td><strong>${c.score}</strong></td>
        <td><span class="badge ${badgeCls}">${c.score_label || ''}</span></td>
        <td>${c.years_experience ?? '—'} años</td>
        <td>${c.availability || '—'}</td>
        <td>${date}</td>
        <td><button class="btn-detail" onclick="openModal(${c.id})">Ver detalle</button></td>
      </tr>`;
    }).join('');
  }

  async function openModal(id) {
    const res = await fetch(`/api/panel/candidatos/${id}`, { headers: { 'X-Recruiter-Password': AUTH } });
    const c = await res.json();
    const initials = c.name.split(' ').map(w => w[0]).slice(0, 2).join('').toUpperCase();
    document.getElementById('mAvatar').textContent = initials;
    document.getElementById('mName').textContent = c.name;
    document.getElementById('mEmail').textContent = c.email || 'Sin email';
    document.getElementById('mSummary').textContent = c.summary || '—';
    document.getElementById('mStrength').textContent = c.strength || '—';
    const wk = document.getElementById('mWeaknesses');
    wk.innerHTML = (c.weaknesses || []).map(w => `<li>${w}</li>`).join('') || '<li>—</li>';
    const sk = document.getElementById('mSkills');
    sk.innerHTML = (c.matching_skills || []).map(s => `<span class="skill-tag">${s}</span>`).join('') || '<span style="color:var(--gray)">—</span>';
    document.getElementById('mAnswers').innerHTML = `
      <div class="answer-row"><span>Disponibilidad</span><span>${c.availability || '—'}</span></div>
      <div class="answer-row"><span>Sueldo esperado</span><span>${c.expected_salary || '—'}</span></div>
      <div class="answer-row"><span>Años de experiencia</span><span>${c.specific_experience || '—'}</span></div>`;
    document.getElementById('mDownloadBtn').onclick = () => {
      window.open(`/api/panel/candidatos/${id}/cv?x_recruiter_password=${AUTH}`, '_blank');
    };
    document.getElementById('modalOverlay').classList.add('open');
  }

  function closeModal() { document.getElementById('modalOverlay').classList.remove('open'); }
  document.getElementById('modalOverlay').addEventListener('click', e => { if (e.target === e.currentTarget) closeModal(); });
</script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add frontend/panel.html
git commit -m "feat: add recruiter dashboard with login and candidate modal"
```

---

## Task 10: frontend/crear-cv.html

**Files:** `frontend/crear-cv.html`

- [ ] **Step 1: Create frontend/crear-cv.html**

```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Crear CV con IA — CVSmart</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    :root { --navy: #1F3864; --blue: #2E75B6; --accent: #4FC3F7; --bg: #F8FAFF; --gray: #64748b; }
    body { font-family: 'Inter', sans-serif; background: var(--bg); color: #1e293b; }
    nav { position: fixed; top: 0; left: 0; right: 0; z-index: 100;
          background: rgba(255,255,255,.9); backdrop-filter: blur(12px);
          border-bottom: 1px solid rgba(31,56,100,.08);
          display: flex; align-items: center; justify-content: space-between;
          padding: 0 2rem; height: 64px; }
    .nav-logo { font-weight: 800; font-size: 1.25rem; color: var(--navy); text-decoration: none; }
    .nav-links { display: flex; gap: 1rem; }
    .nav-links a { text-decoration: none; color: var(--gray); font-weight: 500; }
    .nav-links .active { color: var(--navy); font-weight: 700; }

    main { padding: 96px 1.5rem 4rem; max-width: 860px; margin: 0 auto; }
    .page-title { font-size: 2rem; font-weight: 800; color: var(--navy); margin-bottom: .5rem; }
    .page-sub { color: var(--gray); margin-bottom: 2.5rem; }

    .card { background: rgba(255,255,255,.85); backdrop-filter: blur(12px);
            border-radius: 20px; box-shadow: 0 8px 32px rgba(31,56,100,.12); padding: 2rem; margin-bottom: 1.5rem; }
    .section-label { font-weight: 700; font-size: .8rem; text-transform: uppercase;
                     letter-spacing: .08em; color: var(--blue); margin-bottom: 1rem; }
    label { display: block; font-weight: 600; font-size: .9rem; color: #374151; margin-bottom: 6px; }
    input, select, textarea {
      width: 100%; padding: 11px 14px; border: 1.5px solid #e2e8f0;
      border-radius: 10px; font-size: .95rem; font-family: inherit; color: #1e293b;
      transition: border-color .2s, box-shadow .2s; background: #fff;
    }
    input:focus, select:focus, textarea:focus {
      outline: none; border-color: var(--blue); box-shadow: 0 0 0 3px rgba(46,117,182,.15);
    }
    textarea { resize: vertical; min-height: 90px; }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
    .form-group { margin-bottom: 1.25rem; }
    .divider { border: none; border-top: 1px solid #e2e8f0; margin: 1.25rem 0; }

    /* Experience / Education blocks */
    .exp-block, .edu-block {
      border: 1.5px solid #e2e8f0; border-radius: 14px; padding: 1.25rem; margin-bottom: 1rem;
    }
    .block-title { font-weight: 700; color: var(--navy); font-size: .95rem; margin-bottom: 1rem; }
    .btn-add { display: inline-flex; align-items: center; gap: 6px; padding: 8px 16px;
               background: #eff6ff; color: var(--blue); border: 1.5px solid #bfdbfe;
               border-radius: 8px; font-size: .85rem; font-weight: 600; cursor: pointer; transition: background .2s; }
    .btn-add:hover { background: #dbeafe; }

    /* Tags */
    .tags-input { display: flex; flex-wrap: wrap; gap: 6px; padding: 8px; border: 1.5px solid #e2e8f0;
                  border-radius: 10px; min-height: 48px; cursor: text; background: #fff; }
    .tags-input:focus-within { border-color: var(--blue); box-shadow: 0 0 0 3px rgba(46,117,182,.15); }
    .tag { display: flex; align-items: center; gap: 4px; padding: 4px 10px;
           background: #eff6ff; color: var(--blue); border-radius: 99px; font-size: .82rem; font-weight: 600; }
    .tag button { background: none; border: none; cursor: pointer; color: var(--blue); font-size: 1rem; line-height: 1; padding: 0; }
    .tag-input-field { border: none; outline: none; font-size: .9rem; font-family: inherit;
                       min-width: 120px; flex: 1; padding: 2px 4px; }

    /* Submit */
    .btn-generate { width: 100%; padding: 15px; background: linear-gradient(135deg, var(--navy), var(--blue));
                    color: #fff; border: none; border-radius: 14px; font-size: 1.05rem; font-weight: 700;
                    cursor: pointer; transition: transform .2s, box-shadow .2s; }
    .btn-generate:hover { transform: translateY(-2px); box-shadow: 0 8px 28px rgba(31,56,100,.3); }
    .btn-generate:disabled { opacity: .6; cursor: not-allowed; transform: none; }

    /* Loading */
    .loading { display: none; text-align: center; padding: 2.5rem; }
    .spinner { width: 48px; height: 48px; border: 4px solid #e2e8f0;
               border-top-color: var(--blue); border-radius: 50%;
               animation: spin 1s linear infinite; margin: 0 auto 1rem; }
    @keyframes spin { to { transform: rotate(360deg); } }

    /* Preview */
    .preview-card { display: none; background: #fff; border-radius: 20px;
                    box-shadow: 0 8px 32px rgba(31,56,100,.12); padding: 2.5rem; margin-top: 2rem; }
    .preview-card h2 { color: var(--navy); font-size: 1.5rem; margin-bottom: .25rem; }
    .preview-contact { color: var(--blue); font-size: .9rem; margin-bottom: 1.25rem; }
    .preview-section { margin-bottom: 1.25rem; }
    .preview-section h4 { color: var(--navy); font-weight: 700; font-size: .85rem;
                          text-transform: uppercase; letter-spacing: .06em;
                          border-bottom: 2px solid var(--blue); padding-bottom: 4px; margin-bottom: 10px; }
    .preview-section p { color: #374151; font-size: .9rem; line-height: 1.6; }
    .exp-item { margin-bottom: 10px; }
    .exp-item strong { color: var(--navy); }
    .exp-item ul { padding-left: 1.2rem; color: #4b5563; font-size: .88rem; line-height: 1.7; }
    .preview-tags { display: flex; flex-wrap: wrap; gap: 6px; }
    .preview-tag { padding: 4px 12px; background: #eff6ff; color: var(--blue); border-radius: 99px; font-size: .82rem; font-weight: 600; }
    .btn-download { margin-top: 1.5rem; padding: 13px 32px; background: linear-gradient(135deg, var(--navy), var(--blue));
                    color: #fff; border: none; border-radius: 12px; font-size: 1rem; font-weight: 700;
                    cursor: pointer; transition: transform .2s; }
    .btn-download:hover { transform: translateY(-2px); }

    @media (max-width: 560px) { .form-row { grid-template-columns: 1fr; } }
  </style>
</head>
<body>
<nav>
  <a class="nav-logo" href="/">🧠 CVSmart</a>
  <div class="nav-links">
    <a href="/">Inicio</a>
    <a href="/aplicar">Postularme</a>
    <a href="/crear-cv" class="active">Crear CV</a>
    <a href="/panel">Reclutador</a>
  </div>
</nav>

<main>
  <h1 class="page-title">Creador de CV con IA</h1>
  <p class="page-sub">Llena el formulario y nuestra IA redacta tu CV de forma profesional y lo genera como PDF listo para descargar.</p>

  <form id="cvForm">
    <!-- Datos personales -->
    <div class="card">
      <p class="section-label">Datos personales</p>
      <div class="form-group">
        <label>Nombre completo *</label>
        <input type="text" name="nombre" required placeholder="Ej: Juan Carlos Pérez García" />
      </div>
      <div class="form-row">
        <div class="form-group">
          <label>Correo electrónico</label>
          <input type="email" name="email" placeholder="tu@correo.com" />
        </div>
        <div class="form-group">
          <label>Teléfono</label>
          <input type="tel" name="telefono" placeholder="+52 55 0000 0000" />
        </div>
      </div>
      <div class="form-group">
        <label>Puesto al que aspiras</label>
        <input type="text" name="puesto_objetivo" placeholder="Ej: Desarrollador Full Stack Senior" />
      </div>
      <div class="form-group">
        <label>Resumen profesional</label>
        <textarea name="resumen_profesional" placeholder="Escribe un breve resumen de tu experiencia y objetivos profesionales..."></textarea>
      </div>
    </div>

    <!-- Experiencia -->
    <div class="card">
      <p class="section-label">Experiencia laboral</p>
      <div id="expContainer">
        <div class="exp-block">
          <p class="block-title">Empleo 1</p>
          <div class="form-row">
            <div class="form-group"><label>Empresa</label><input type="text" class="exp-empresa" placeholder="Nombre de la empresa" /></div>
            <div class="form-group"><label>Puesto</label><input type="text" class="exp-puesto" placeholder="Tu cargo" /></div>
          </div>
          <div class="form-group"><label>Periodo</label><input type="text" class="exp-periodo" placeholder="Ej: Enero 2021 – Diciembre 2023" /></div>
          <div class="form-group"><label>Descripción de actividades</label><textarea class="exp-desc" placeholder="Describe tus principales responsabilidades y logros..."></textarea></div>
        </div>
      </div>
      <button type="button" class="btn-add" id="addExpBtn">+ Agregar otro empleo</button>
      <p style="font-size:.8rem; color:var(--gray); margin-top:6px">Máximo 3 empleos</p>
    </div>

    <!-- Educación -->
    <div class="card">
      <p class="section-label">Educación</p>
      <div id="eduContainer">
        <div class="edu-block">
          <p class="block-title">Nivel 1</p>
          <div class="form-row">
            <div class="form-group"><label>Institución</label><input type="text" class="edu-inst" placeholder="Nombre de la escuela/universidad" /></div>
            <div class="form-group"><label>Carrera / Grado</label><input type="text" class="edu-carrera" placeholder="Ej: Ingeniería en Sistemas" /></div>
          </div>
          <div class="form-group" style="max-width:200px"><label>Año de graduación</label><input type="text" class="edu-anio" placeholder="Ej: 2020" /></div>
        </div>
      </div>
      <button type="button" class="btn-add" id="addEduBtn">+ Agregar otro nivel</button>
      <p style="font-size:.8rem; color:var(--gray); margin-top:6px">Máximo 2 niveles</p>
    </div>

    <!-- Habilidades -->
    <div class="card">
      <p class="section-label">Habilidades</p>
      <div class="form-group">
        <label>Habilidades técnicas <span style="font-weight:400; color:var(--gray)">(escribe y presiona Enter)</span></label>
        <div class="tags-input" id="techTags" data-tags="[]">
          <input type="text" class="tag-input-field" id="techInput" placeholder="Ej: Python, React, SQL..." />
        </div>
      </div>
      <div class="form-group">
        <label>Habilidades blandas <span style="font-weight:400; color:var(--gray)">(escribe y presiona Enter)</span></label>
        <div class="tags-input" id="softTags" data-tags="[]">
          <input type="text" class="tag-input-field" id="softInput" placeholder="Ej: Liderazgo, Comunicación..." />
        </div>
      </div>
      <div class="form-group">
        <label>Idiomas y nivel <span style="font-weight:400; color:var(--gray)">(escribe y presiona Enter)</span></label>
        <div class="tags-input" id="langTags" data-tags="[]">
          <input type="text" class="tag-input-field" id="langInput" placeholder="Ej: Español (nativo), Inglés (B2)..." />
        </div>
      </div>
    </div>

    <button type="submit" class="btn-generate" id="generateBtn">✨ Generar CV con IA</button>
  </form>

  <div class="loading" id="loadingState">
    <div class="spinner"></div>
    <p style="color:var(--gray); font-size:.95rem" id="loadingMsg">La IA está redactando tu CV...</p>
  </div>

  <!-- PREVIEW -->
  <div class="preview-card" id="previewCard">
    <h2 id="pvNombre"></h2>
    <p class="preview-contact" id="pvContacto"></p>
    <div class="preview-section" id="pvPuesto"></div>
    <div class="preview-section" id="pvResumen"></div>
    <div class="preview-section" id="pvExp"></div>
    <div class="preview-section" id="pvEdu"></div>
    <div class="preview-section" id="pvTech"></div>
    <div class="preview-section" id="pvSoft"></div>
    <div class="preview-section" id="pvLang"></div>
    <button class="btn-download" id="downloadBtn">⬇️ Descargar PDF</button>
  </div>
</main>

<script>
  // ── Tag inputs ────────────────────────────────────────────────────
  function initTagInput(containerId, inputId) {
    const container = document.getElementById(containerId);
    const input = document.getElementById(inputId);
    input.addEventListener('keydown', e => {
      if ((e.key === 'Enter' || e.key === ',') && input.value.trim()) {
        e.preventDefault();
        addTag(container, input.value.trim());
        input.value = '';
      }
    });
    container.addEventListener('click', () => input.focus());
  }

  function addTag(container, text) {
    const tag = document.createElement('div');
    tag.className = 'tag';
    tag.innerHTML = `${text}<button type="button" onclick="this.parentElement.remove()">×</button>`;
    container.insertBefore(tag, container.querySelector('.tag-input-field'));
  }

  function getTags(containerId) {
    return [...document.getElementById(containerId).querySelectorAll('.tag')]
      .map(t => t.textContent.replace('×', '').trim());
  }

  initTagInput('techTags', 'techInput');
  initTagInput('softTags', 'softInput');
  initTagInput('langTags', 'langInput');

  // ── Dynamic blocks ────────────────────────────────────────────────
  let expCount = 1, eduCount = 1;

  document.getElementById('addExpBtn').addEventListener('click', () => {
    if (expCount >= 3) return;
    expCount++;
    const div = document.createElement('div');
    div.className = 'exp-block';
    div.innerHTML = `<p class="block-title">Empleo ${expCount}</p>
      <div class="form-row">
        <div class="form-group"><label>Empresa</label><input type="text" class="exp-empresa" placeholder="Nombre de la empresa" /></div>
        <div class="form-group"><label>Puesto</label><input type="text" class="exp-puesto" placeholder="Tu cargo" /></div>
      </div>
      <div class="form-group"><label>Periodo</label><input type="text" class="exp-periodo" placeholder="Ej: Enero 2021 – Diciembre 2023" /></div>
      <div class="form-group"><label>Descripción de actividades</label><textarea class="exp-desc" placeholder="Describe tus principales responsabilidades..."></textarea></div>`;
    document.getElementById('expContainer').appendChild(div);
    if (expCount >= 3) document.getElementById('addExpBtn').style.display = 'none';
  });

  document.getElementById('addEduBtn').addEventListener('click', () => {
    if (eduCount >= 2) return;
    eduCount++;
    const div = document.createElement('div');
    div.className = 'edu-block';
    div.innerHTML = `<p class="block-title">Nivel ${eduCount}</p>
      <div class="form-row">
        <div class="form-group"><label>Institución</label><input type="text" class="edu-inst" placeholder="Nombre de la escuela" /></div>
        <div class="form-group"><label>Carrera / Grado</label><input type="text" class="edu-carrera" placeholder="Ej: Maestría en Administración" /></div>
      </div>
      <div class="form-group" style="max-width:200px"><label>Año</label><input type="text" class="edu-anio" placeholder="Ej: 2022" /></div>`;
    document.getElementById('eduContainer').appendChild(div);
    if (eduCount >= 2) document.getElementById('addEduBtn').style.display = 'none';
  });

  // ── Form submit ───────────────────────────────────────────────────
  let lastCvData = null;

  document.getElementById('cvForm').addEventListener('submit', async e => {
    e.preventDefault();
    const form = e.target;

    const experiencia = [...document.querySelectorAll('.exp-block')].map(b => ({
      empresa: b.querySelector('.exp-empresa').value,
      puesto:  b.querySelector('.exp-puesto').value,
      periodo: b.querySelector('.exp-periodo').value,
      logros:  b.querySelector('.exp-desc').value.split('\n').filter(l => l.trim()),
    })).filter(x => x.empresa || x.puesto);

    const educacion = [...document.querySelectorAll('.edu-block')].map(b => ({
      institucion: b.querySelector('.edu-inst').value,
      carrera:     b.querySelector('.edu-carrera').value,
      año:         b.querySelector('.edu-anio').value,
    })).filter(x => x.institucion || x.carrera);

    const payload = {
      nombre: form.nombre.value,
      contacto: { email: form.email.value, telefono: form.telefono.value },
      puesto_objetivo: form.puesto_objetivo.value,
      resumen_profesional: form.resumen_profesional.value,
      experiencia, educacion,
      habilidades_tecnicas: getTags('techTags'),
      habilidades_blandas: getTags('softTags'),
      idiomas: getTags('langTags'),
    };

    document.getElementById('cvForm').style.display = 'none';
    document.getElementById('loadingState').style.display = 'block';

    const msgs = ['La IA está redactando tu CV...', 'Mejorando la redacción profesional...', 'Generando el PDF...'];
    let mi = 0;
    const t = setInterval(() => { document.getElementById('loadingMsg').textContent = msgs[Math.min(++mi, msgs.length-1)]; }, 2500);

    try {
      const res = await fetch('/api/generar-cv', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload),
      });
      clearInterval(t);

      if (!res.ok) {
        const err = await res.json();
        document.getElementById('loadingState').style.display = 'none';
        document.getElementById('cvForm').style.display = 'block';
        alert(err.detail || 'Error al generar el CV');
        return;
      }

      // Response is PDF — create blob download
      const blob = await res.blob();
      lastCvData = { blob, nombre: payload.nombre };

      document.getElementById('loadingState').style.display = 'none';
      renderPreview(payload);
    } catch {
      clearInterval(t);
      document.getElementById('loadingState').style.display = 'none';
      document.getElementById('cvForm').style.display = 'block';
      alert('Error de conexión.');
    }
  });

  function renderPreview(cv) {
    document.getElementById('pvNombre').textContent = cv.nombre;
    document.getElementById('pvContacto').textContent = `${cv.contacto.email}  |  ${cv.contacto.telefono}`;
    if (cv.puesto_objetivo) document.getElementById('pvPuesto').innerHTML = `<h4>Puesto objetivo</h4><p>${cv.puesto_objetivo}</p>`;
    if (cv.resumen_profesional) document.getElementById('pvResumen').innerHTML = `<h4>Resumen Profesional</h4><p>${cv.resumen_profesional}</p>`;
    if (cv.experiencia?.length) {
      document.getElementById('pvExp').innerHTML = '<h4>Experiencia Laboral</h4>' +
        cv.experiencia.map(x => `<div class="exp-item"><strong>${x.puesto}</strong> — ${x.empresa} (${x.periodo})<ul>${(x.logros||[]).map(l=>`<li>${l}</li>`).join('')}</ul></div>`).join('');
    }
    if (cv.educacion?.length) {
      document.getElementById('pvEdu').innerHTML = '<h4>Educación</h4>' +
        cv.educacion.map(x => `<p><strong>${x.carrera}</strong> — ${x.institucion} (${x.año})</p>`).join('');
    }
    if (cv.habilidades_tecnicas?.length) document.getElementById('pvTech').innerHTML = '<h4>Habilidades Técnicas</h4><div class="preview-tags">' + cv.habilidades_tecnicas.map(s=>`<span class="preview-tag">${s}</span>`).join('') + '</div>';
    if (cv.habilidades_blandas?.length) document.getElementById('pvSoft').innerHTML = '<h4>Habilidades Blandas</h4><div class="preview-tags">' + cv.habilidades_blandas.map(s=>`<span class="preview-tag">${s}</span>`).join('') + '</div>';
    if (cv.idiomas?.length) document.getElementById('pvLang').innerHTML = '<h4>Idiomas</h4><div class="preview-tags">' + cv.idiomas.map(s=>`<span class="preview-tag">${s}</span>`).join('') + '</div>';
    document.getElementById('previewCard').style.display = 'block';
    document.getElementById('previewCard').scrollIntoView({ behavior: 'smooth' });
  }

  document.getElementById('downloadBtn').addEventListener('click', () => {
    if (!lastCvData) return;
    const url = URL.createObjectURL(lastCvData.blob);
    const a = document.createElement('a');
    a.href = url; a.download = `CV-${lastCvData.nombre.replace(/ /g,'_')}.pdf`;
    a.click(); URL.revokeObjectURL(url);
  });
</script>
</body>
</html>
```

- [ ] **Step 2: Commit**

```bash
git add frontend/crear-cv.html
git commit -m "feat: add AI CV creator with dynamic form and PDF download"
```

---

## Task 11: Final cleanup + smoke test

**Files:** Remove old Node.js files

- [ ] **Step 1: Run all tests**

```bash
pytest tests/ -v
```

Expected: all tests PASS (database, analyzer, email_sender, cv_generator).

- [ ] **Step 2: Start server and smoke test**

```bash
uvicorn main:app --port 8000 --reload
```

Then open:
- `http://localhost:8000/` — landing page should load with Inter font and navy design
- `http://localhost:8000/aplicar` — form with drag & drop and select for availability
- `http://localhost:8000/panel` — login screen
- `http://localhost:8000/crear-cv` — CV creator form

- [ ] **Step 3: Remove old Node.js files**

```bash
rm -f server.js services/geminiService.js services/claudeService.js services/pdfService.js
rm -f routes/candidates.js routes/recruiter.js config/database.js
rmdir services routes config 2>/dev/null || true
```

- [ ] **Step 4: Final commit**

```bash
git add -A
git commit -m "chore: remove old Node.js backend, CVSmart V2 complete"
```

---

## Quick Start Checklist

Before running, configure `.env`:

```
GROQ_API_KEY=         ← required for CV analysis and CV generator
SECRET_PANEL=cvsmart2026
GMAIL_USER=           ← optional, emails won't send without this
GMAIL_APP_PASSWORD=   ← Gmail App Password (not regular password)
```

**Run:**
```bash
pip install -r requirements.txt
uvicorn main:app --port 8000
```

Open: `http://localhost:8000`
